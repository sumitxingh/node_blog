version: '3.8'

services:
  postgres:
    image: postgres:17,2
    container_name: postgres_service
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
